query getCities($city: String, $today: DateTime) {
  city: allCities(filter: { name: $city }, orderBy: name_ASC) {
    id
    name
    weather(filter: { datetime: $today }) {
      id
      datetime
      precipitationMm
      precipitationProbability
      temperatureMax
      temperatureMin
    }
  }
}
